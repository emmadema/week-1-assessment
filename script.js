//used to make the color appear on 1/3 of the page instead of 1/2 


//window.addEventListener("DOMContentLoaded", setBodyGradientOnLoad); // as the page uploads run this color

//function setBodyGradientOnLoad() {
    //var heightPx = window.innerHeight;
   // var gradientStop = Math.floor( heightPx * 0.3 ); // this is the 1/3
   // var body = document.getElementsByTagName("body")[0];
   // body.style.backgroundImage = "linear-gradient(to bottom, #6283a1, #4b7193  " + gradientStop + "px, white " + gradientStop + "px, white)";
//